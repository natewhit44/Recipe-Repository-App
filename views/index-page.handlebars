{{> header}}

<main>
	{{> left-navbar}}

  <div class="recipe-workspace">
  		{{> home-page}}

    	{{> contact-page}}

			<h1 class="recipe-header">List of Recipes:</h1>
<<<<<<< HEAD

			<div id="show-recipe">SHOW</div>
=======
			{{#if recipeContent}}
			<div id="show-recipe">Click here</div>
			{{/if}}
>>>>>>> d5d5582231fe938aba8556cae31b7c39f613ae35
			<!-- <h3>Title: {{title}}</h3>
			<h4>Recipes:</h4> -->

			{{#each recipes}}
				{{> recipe-icon-title categoryName = this.recipe_category}}
			{{/each}}

{{!--         <h1>Test Zone: {{title}}</h1>
        {{#each search_results}}
            <p>{{this.recipe_name}}</p>
            <p>{{this.recipe_category}}</p>
        {{/each}} --}}

		<div class="recipe-list">
	{{!-- 		{{#if recipes}}
				{{#each recipes}}
					{{> recipe-icon-title categoryName = ../title}}
				{{/each}}
			{{else}}
				{{#if category}}
					{{#each category}}
						<h3>{{@key}}</h3>
						{{#each this.recipes}}
							<p>{{this.title}}</p>
						{{/each}}
					{{/each}}
				{{/if}}
			{{/if}} --}}
		</div>
	</div>


  <div class="right-frame"></div>

  <div id="search-box">
    <form id="searchForm">
        <input id="search-box-input" name="query" placeholder="Enter search query...">
   </form>
  </div>



	{{> google-search}}

</main>

{{> footer}}

<button type="button" class="google-search">Google Search?</button>

{{> recipe-display content = recipeContent}}
