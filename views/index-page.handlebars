{{> header}}

<main>
	{{> left-navbar}}

  <div class="recipe-workspace">
{{!--   		 {{> home-page}}

    	{{> contact-page}}
 --}}
			<h1 class="recipe-header">List of Recipes:</h1>

			<h3>Title: {{title}}</h3>
			<h4>Recipes:</h4>
			{{#each recipes}}
				{{> recipe-icon-title categoryName = this.recipe_category}}
			{{/each}}

        <h1>Test Zone{{success}}</h1>
  {{!--       {{#each success}}
          <p>Target Area:
              {{this.value}}
          </p>
        {{/each}} --}}


	 	{{!--  <h3>Main Recipe Info: </h3>
    	{{#each recipeContent.recipeMain}}
    		<p>{{this.recipe_name}}
    		{{this.recipe_category}}
    		{{this.prep_time}}
    		{{this.cook_time}}
    		{{this.temp}}
    		{{this.yield}}
    		</p>
    	{{/each}}

    	<h3>Equipment: </h3>
    	{{#each recipeContent.equipment}}
    		<p>{{this.equipment_piece}}
    		</p>
    	{{/each}}

    	<h3>Ingredients: </h3>
    	{{#each recipeContent.ingredients}}
    		<p>{{this.ingredient_value}}
    		</p>
    	{{/each}}

    	<h3>Steps: </h3>
    	{{#each recipeContent.steps}}
    		<p>{{this.step_value}}
    		</p>
    	{{/each}} --}}


		<div class="recipe-list">
	{{!-- 		{{#if recipes}}
				{{#each recipes}}
					{{> recipe-icon-title categoryName = ../title}}
				{{/each}}
			{{else}}
				{{#if category}}
					{{#each category}}
						<h3>{{@key}}</h3>
						{{#each this.recipes}}
							<p>{{this.title}}</p>
						{{/each}}
					{{/each}}
				{{/if}}
			{{/if}} --}}
		</div>
	</div>


  <div class="right-frame"></div>

  <div id="search-box">
    <form id="searchForm">
        <input id="search-box-input" type="text" name="query" placeholder="Enter search query...">
   </form>
  </div>



	{{> google-search}}

</main>

{{> footer}}

<button type="button" class="google-search">Google Search?</button>

{{> recipe-display content = recipeContent}}
